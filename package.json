{
  "name": "@spolander/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "server": "cd packages/wine-server && yarn dev",
    "client": "cd packages/wine-client && yarn start"
  }
}
