{
  "root": "apps/wine-server",
  "sourceRoot": "apps/wine-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/wine-server",
        "main": "apps/wine-server/src/main.ts",
        "tsConfig": "apps/wine-server/tsconfig.app.json",
        "assets": ["apps/wine-server/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/wine-server/src/environments/environment.ts",
              "with": "apps/wine-server/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "wine-server:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/wine-server/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/wine-server"],
      "options": {
        "jestConfig": "apps/wine-server/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
